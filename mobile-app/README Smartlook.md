# SmartLook-AI — App Móvil (Flutter)

Generar el proyecto (requiere Flutter SDK):

```bash
cd mobile-app
flutter create .
```

Pantallas sugeridas: `catalogo/`, `disponibilidad/`, `vestidor_ar/`, `reservas/`, `carrito_checkout/`, `perfil_historial/`.
